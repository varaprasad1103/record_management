package com.records.recordmanagement.repository;

import com.records.recordmanagement.model.RiceMill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RiceMillRepository extends JpaRepository<RiceMill, Long> {
}
